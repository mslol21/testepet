# React Custom Hooks

Este diretório expõe ganchos utilitários reutilizáveis pelos componentes visuais:
- `useTenant`: Hook para obter o tenant ativo, aplicando as cores de marca customizadas.
- `useAuth`: Hook de autenticação (Firebase ou Local Storage Mock) com perfilagem de papéis de acesso (roles).
